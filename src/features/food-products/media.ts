import type { MediaRecordType } from "@/features/media/recordTypes";

/** media_assets.record_type に使う識別子（猫に紐付かないため cat_id は null） */
export const FOOD_PRODUCT_MEDIA_TYPE: MediaRecordType = "food_product";
